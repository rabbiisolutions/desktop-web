import React from "react";
import Icon from "./icon";
import next from "../../icons/howTo/forward-solid.svg";

const Next = () => {
  return (
      <div className="next-icon">
        <Icon src={next} height={2.5}/>
      </div>
  );
};

// Exporting the component
export default Next;
